numeric_vector <- c(1, 2, 3, 4, 5)
character_vector <- c("apple", "banana", "cherry")

nested_list <- list(
  name = "Fruit and Numbers",
  data = list(
    numbers = numeric_vector,
    fruits = character_vector
  )
)
nested_list
